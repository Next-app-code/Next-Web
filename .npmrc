# Ignore optional dependencies that may cause build issues
optional=false

# Disable prepare scripts that might fail in serverless environments
ignore-scripts=false

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true




